## Installation
- Use `h -c "$(curl -fsLS get.chezmoi.io)" -- init --apply jorge-mells/dotfiles-2` to install the repo and chezmoi.
- Run `chezmoi apply` to configure the terminal. The setup script will prompt you to install dependencies. Use `a` to skip this when prompted. See `dot_config/bash/installs` for the list of dependencies.
